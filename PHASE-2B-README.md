# KULT360 Phase 2B patch

Extract this archive at the root of `/workspaces/Kult360`. It adds supplemental files under `migration/source-assets/saveweb2zip/` and does not overwrite the existing Phase 2 source assets.

Run:

```bash
node migration/scripts/validate-phase-2b.mjs
```

Do not deploy or import to Sanity yet.
